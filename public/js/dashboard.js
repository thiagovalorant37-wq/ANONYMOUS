const root=document.getElementById('root');
function showLogin(){root.innerHTML=`<div class="grid-bg"></div><div class="vignette"></div><div class="login-wrap"><section class="login"><div class="eyebrow">Área restrita</div><h1>Backend Complexo RP</h1><p class="muted">Central administrativa do site</p><input class="input" id="pw" type="password" placeholder="Senha do backend"><button class="btn" id="login">Entrar no painel</button><p class="error" id="err"></p></section></div>`;document.getElementById('login').onclick=async()=>{try{await login(document.getElementById('pw').value)}catch(e){document.getElementById('err').textContent=e.message}}}
async function boot(){if(!token())return showLogin();try{const [r,o,s]=await Promise.all([api('/api/admin/recruitment'),api('/api/admin/orders'),fetch('/api/stock').then(x=>x.json())]);const rec=r.items,ord=o.items,stock=s.stock;const pending=rec.filter(x=>x.status==='pendente').length+ord.filter(x=>x.status==='pendente').length;root.innerHTML=layout('dashboard',`<div class="topbar"><div><div class="eyebrow">Control Center</div><h1 class="title">Dashboard</h1><div class="muted">Visão geral do Complexo RP</div></div><button class="btn secondary" onclick="logout()">Sair</button></div><section class="grid"><div class="card stat"><small>TICKETS TOTAIS</small><div class="num">${rec.length+ord.length}</div><span class="muted">Todas as categorias</span></div><div class="card stat"><small>RECRUTAMENTOS</small><div class="num">${rec.length}</div><span class="muted">Solicitações recebidas</span></div><div class="card stat"><small>ENCOMENDAS</small><div class="num">${ord.length}</div><span class="muted">Pedidos registrados</span></div><div class="card stat"><small>PENDENTES</small><div class="num">${pending}</div><span class="muted">Aguardando atendimento</span></div></section><section class="card" style="margin-top:16px"><div class="topbar"><div><h2>Atalhos administrativos</h2><div class="muted">Cada módulo possui sua própria página HTML.</div></div></div><div class="grid"><a class="card" href="/admin/tickets.html">🎫<h3>Todos os Tickets</h3><span class="muted">Visualize cada solicitação em detalhes.</span></a><a class="card" href="/admin/recrutamentos.html">👥<h3>Recrutamentos</h3><span class="muted">Análise e decisão dos candidatos.</span></a><a class="card" href="/admin/encomendas.html">📦<h3>Encomendas</h3><span class="muted">Controle pedidos e entregas.</span></a><a class="card" href="/admin/estoque.html">📊<h3>Estoque</h3><span class="muted">Ajuste o estoque dos produtos.</span></a></div></section>`)}catch(e){showLogin()}}boot();
