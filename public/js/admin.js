const app=document.getElementById('app'); const token=localStorage.getItem('complexo_admin_token');
const css='<style>.admin{max-width:1200px;margin:auto;padding:40px 20px}.top{display:flex;justify-content:space-between;align-items:center;gap:20px;margin-bottom:30px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px}.card{background:#141414;border:1px solid #292929;border-radius:14px;padding:22px}.big{font-size:32px;font-weight:800}.nav{display:flex;flex-wrap:wrap;gap:10px;margin:20px 0}.nav a{padding:12px 16px;border:1px solid #333;border-radius:10px;color:#fff;text-decoration:none}.btn{padding:11px 16px;border:0;border-radius:8px;background:#d71920;color:#fff;font-weight:700;cursor:pointer}.login{max-width:420px;margin:12vh auto;background:#141414;padding:30px;border-radius:16px}.login input{width:100%;box-sizing:border-box;padding:12px;margin:10px 0 15px;background:#090909;color:#fff;border:1px solid #333;border-radius:8px}</style>';
function login(){app.innerHTML=css+'<section class="login"><h1>Complexo RP — Backend</h1><p>Central administrativa</p><input id="pw" type="password" placeholder="Senha do backend"><button class="btn" id="go">Entrar</button><p id="err"></p></section>';document.getElementById('go').onclick=async()=>{let r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:document.getElementById('pw').value})});let d=await r.json();if(!r.ok)return document.getElementById('err').textContent=d.error;localStorage.setItem('complexo_admin_token',d.token);location.reload()}}
async function load(){let h=await fetch('/api/health');if(!token)return login();let auth={Authorization:'Bearer '+token};let [r,o,s]=await Promise.all([fetch('/api/admin/recruitment',{headers:auth}),fetch('/api/admin/orders',{headers:auth}),fetch('/api/stock')]);if(r.status===401){localStorage.removeItem('complexo_admin_token');return login()}let rec=(await r.json()).items, ord=(await o.json()).items, stock=(await s.json()).stock;app.innerHTML=css+`<div class="top"><div><h1>Central de Backend</h1><p>Complexo RP — Tickets e Gestão</p></div><button class="btn" onclick="logout()">Sair</button></div><div class="nav"><a href="/admin.html">Dashboard</a><a href="/admin/recrutamentos.html">🎫 Recrutamentos</a><a href="/admin/encomendas.html">📦 Encomendas</a><a href="/admin/estoque.html">📊 Estoque</a></div><div class="grid"><div class="card"><div class="big">${rec.length}</div>Recrutamentos</div><div class="card"><div class="big">${ord.length}</div>Encomendas</div><div class="card"><div class="big">${rec.filter(x=>x.status==='pendente').length}</div>Recrutamentos pendentes</div><div class="card"><div class="big">${ord.filter(x=>x.status==='pendente').length}</div>Encomendas pendentes</div></div><div class="card" style="margin-top:20px"><h2>Tickets recentes</h2>${[...rec.map(x=>({...x,_cat:'RECRUTAMENTO'})),...ord.map(x=>({...x,_cat:'ENCOMENDA'}))].sort((a,b)=>b.createdAt-a.createdAt).slice(0,10).map(x=>`<div style="padding:12px 0;border-bottom:1px solid #292929"><b>${x.id}</b> — ${x._cat}<br><span class="muted">${new Date(x.createdAt).toLocaleString('pt-BR')} • ${x.status}</span></div>`).join('')}</div>`}
function logout(){localStorage.removeItem('complexo_admin_token');location.reload()} load();
